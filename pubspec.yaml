name: wifi_analyzer
description: Mobile Wi-Fi Security Auditor — passive scanner with risk scoring and education.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  wifi_scan: ^0.4.1
  permission_handler: ^11.3.0
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
